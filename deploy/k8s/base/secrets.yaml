# 默认凭据仅用于本地/测试。生产环境请替换为 Sealed Secrets / External Secrets /
# CSI 驱动，不要将明文密钥提交到仓库。
apiVersion: v1
kind: Secret
metadata:
  name: maxkey-secrets
  namespace: maxkey
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: maxkey
  DATABASE_NAME: maxkey
  DATABASE_USER: root
  DATABASE_PWD: maxkey
